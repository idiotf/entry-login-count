{
  "name": "entry-login-count",
  "module": "src/index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "watch": "bun build --watch --sourcemap=inline --no-clear-screen --minify --format=iife --outdir=build/dist --outfile=build/dist/index.js src/index.ts",
    "build": "bun build --minify --format=iife --outdir=build/dist --outfile=build/dist/index.js src/index.ts"
  },
  "type": "module"
}
